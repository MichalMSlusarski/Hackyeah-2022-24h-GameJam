using UnityEngine;

public class TileDeckManager : MonoBehaviour
{
    /*[SerializeField] GameObject[] cards; 
    string cardTag = "Card";
    
    void Start()
    {
        FindAllCards();
    }

    public void MoveTilesIn()
    {
        for (int i = 0; i < cards.Length; i++)
        {
            cards[i].GetComponent<TileCard>().MoveIn();
        }
    }

    public void MoveTilesOut()
    {
        DeselectAllCards();
        
        for (int i = 0; i < cards.Length; i++)
        {
            cards[i].GetComponent<TileCard>().MoveOut();
        }
    }

    void DeselectAllCards()
    {
        for (int i = 0; i < cards.Length; i++)
        {
            cards[i].GetComponent<TileCard>().Deselect();
        }
    }

    void FindAllCards()
    {
        cards = GameObject.FindGameObjectsWithTag(cardTag);

        for (int i = 0; i < cards.Length; i++)
        {
           cards[i].SetActive(false);
        }
    }*/
}
